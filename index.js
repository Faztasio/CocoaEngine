  var cocoa = {};

  cocoa.lex = function(str) {
    var methods = str.split(';');
    methods.forEach(function(method) {
      var token = method.split(' ');
      if(token[0] == 'print') {

      }
    })
  }
